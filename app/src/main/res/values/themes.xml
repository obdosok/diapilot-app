<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- AppCompat parent because MainActivity is an AppCompatActivity (per-app
         language). Compose draws the whole UI and enableEdgeToEdge() owns the
         system bars, so the light, no-action-bar look is unchanged. -->
    <style name="Theme.DiaPilot" parent="Theme.AppCompat.Light.NoActionBar" />
</resources>
